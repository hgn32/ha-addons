## 1.6.0.23

- バージョン表記を `1.6.0-22` から `1.6.0.23` へ変更。`-` を含むと HA が使う
  `AwesomeVersion` が以降を SemVer のプレリリース修飾子として扱い、
  `1.6.0-23 > 1.6.0-22` が False になるため、更新を検知できなくなっていた
  （アドオンの中身は変わっていない）

## 1.6.0-22

- Tomcat（Guacamole Web）の最大メモリ使用量を設定できる `max_memory_mb` オプションを追加（既定 1024MB）
