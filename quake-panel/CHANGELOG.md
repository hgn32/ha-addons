## 0.3.7

- **パネルの設定画面のバージョン表記が「開発版」のままだったのを直しました。**
  いま動いているのがどのビルドなのか画面から分からず、更新したつもりで古いまま
  でも気づけませんでした。上流のコミットハッシュとビルド日時が出ます
  （例: `バージョン: 2b75756 (2026-09-09 15:13 ビルド)`）
  - 原因はアドオンのイメージ作成でハッシュを渡していなかったことです。上流は
    環境変数 `COMMIT_HASH` か `git` からハッシュを取りますが、アドオンのビルドは
    どちらも使えない状態でした（`.git` を含めず、イメージにも `git` が無いため）
- **上流を最新（`2b75756`）に更新しました。**
  - **緊急地震速報の発表中に出していた「予想震度」の重ね描きを廃止しました。**
    配信データの解像度が 1 画素 ≒ 4.6km しかなく、拡大すると「にじんで海岸線から
    はみ出す」か「巨大なブロックになる」かのどちらかにしかならないためです。
    同じ情報はパネルの「予想最大震度」の大型表示に出ています
    （**アドオンの設定タブの「地図に出す指標」には元から無い項目なので、そちらの
    設定は変わりません**）
  - バージョン表記にビルド日時が並ぶようになりました
- **地震イベントの記録（JSONL）を残すようになりました。** 上流が追加したもので、
  あとから「実際に何が起きたか」を事実で追えるようにするためのものです
  - 平常時は何も書きません。緊急地震速報・地震情報・津波・webhook の送信結果
    などを、地震があったときだけ 1 行ずつ書きます
  - 置き場はアドオンの永続領域（`/data/logs`、30 日で自動削除）です。上流の既定
    のままだとコンテナ内の消える場所に溜まるため、こちらで向け直しています
  - **普段の利用で見る必要はありません。** 不具合を追うときの材料です

## 0.3.6

- **1 回の地震で緊急地震速報のイベントが 3 回発火していたのを直しました（0.3.5 の
  不具合）。** `quake_panel_eew` の `kind: new` が約 60 秒間隔で 3 回飛び、
  `binary_sensor.quake_panel_eew` も `on` → `off` を 3 往復していました。
  **状態変化やイベントをトリガーにしたオートメーションが 3 回走ります**。
  0.3.5 を入れている場合はこの版へ上げてください
  - 原因は 0.3.5 で入れた「最終報は 60 秒で表示終了」です。強震モニタは最終報の
    あとも同じ電文を約 3.5 分返し続けるため、表示終了の直後に同じ電文がまた届き、
    「発表中の地震が無い」→「新規発表」と誤認して発火し直していました
    （2026-09-07 23:21 熊本県天草・芦北地方の緊急地震速報で実測。報 1〜4 は
    すべて同じ地震です）
  - 表示を終えた地震を覚えておき、その焼き直し（同じ地震・報数が増えていない・
    取消でもない）を捨てるようになりました。**報数が増えた続報と、あとから来た
    取消報は今までどおり流れます**（誤報の周知は表示終了後でも必要なため）
- **上流を最新（`e42bd1b`）に更新しました。**
  - **予想震度が格子状の点の並びに見えていたのを直しました。** 0.3.5 で
    「予想震度は観測点ごとの点」と判断したのが誤りで、実際は画素ごとに値を持つ
    「面」でした。面として描き直し、補間だけ切ってあります（1 画素が示す
    約 4.4km 四方そのものとして出るので、海岸線からのはみ出しも隙間も出ません）
  - **予測円（P 波・S 波）を配信画像でなく自前のベクタで描くようになりました。**
    拡大しても線がぼけず、階段状になりません。焼き込まれていた「P」「S」の文字と
    震央マーカーも出なくなります

## 0.3.5

- **上流を最新（`a7d6c37`）に更新しました。**
  - **緊急地震速報の予想震度の表示位置が直りました。** 配信画像を拡大していたため
    1 点が約 20〜28px ににじみ、海岸線からはみ出していました。観測点ごとの点として
    一定の大きさで描くようになります
  - **予測円に焼き込まれていた巨大な × 印（震央マーカー）が出なくなりました。**
    パネル自身の震央マーカーと二重になっていたものです
  - **最終報を受けた緊急地震速報の表示終了が早くなりました（既定 60 秒）。**
    強震モニタは最終報のあとも同じ内容を約 3.5 分返し続けるため、これまでは
    発震から表示終了まで 6 分を超えていました。`binary_sensor.quake_panel_eew` が
    `off` に戻り、`kind: expired` のイベントが流れるのも同じだけ早くなります
  - **画面明滅を続ける時間を「発表時刻」から数えるようになりました。** これまでは
    「その画面が受け取った時刻」からだったため、発表の数分後に開いた端末が
    最初から光り直していました
  - **観測点の位置が直りました。** 防災科学技術研究所の公式観測点リスト
    （K-NET + KiK-net 1,749 点）の座標で描くようになり、密集地で観測点が格子状に
    並んだり、海上に点が出たりしていたのが解消します。毎フレームの画像走査が
    不要になり、描画も軽くなります
  - **地図をクリックすると最寄りの観測点名が出るようになりました。**
    別の観測点を押せば置き換わり、それ以外の場所を押すと消えます
  - 画面左下のクレジット表示が「強震モニタ・観測点情報 (防災科学技術研究所)」に
    変わりました（観測点リストの出典を含めるため）
